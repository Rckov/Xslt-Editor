<ct:Window x:Class="XsltEditor.Views.Windows.Main.SettingsView"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"

           xmlns:ct="clr-namespace:XsltEditor.Views.UserControls"

           Height="605"
           Width="400"
           Title="Settings"

           ResizeMode="CanResizeWithGrip"
           WindowStartupLocation="CenterScreen"
           Style="{StaticResource WindowStyle}" AcrylicEffect="False">

   <Border Style="{StaticResource BorderWindow}">
      <Grid>
         <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
         </Grid.RowDefinitions>

         <ct:WindowBar Grid.Row="0" Background="{DynamicResource GeneralPanelBackground}" Title="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}" ShowTitle="True" />

         <ScrollViewer Grid.Row="1" Margin="10" VerticalScrollBarVisibility="Auto">
            <StackPanel>
               <GroupBox Header="Appearance" Margin="0,0,0,10">
                  <Grid Margin="5">
                     <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100" />
                        <ColumnDefinition />
                     </Grid.ColumnDefinitions>

                     <TextBlock Text="Theme:" Grid.Column="0" VerticalAlignment="Center" Foreground="{DynamicResource TextForeground}" />
                     <ComboBox Grid.Column="1" ItemsSource="{Binding Themes}" SelectedItem="{Binding SelectedTheme}" />
                  </Grid>
               </GroupBox>

               <TextBlock Text="Text Editor Settings" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" Foreground="{DynamicResource TextForeground}" />

               <GroupBox Header="Font" Margin="0,0,0,10">
                  <StackPanel>
                     <Grid Margin="5">
                        <Grid.ColumnDefinitions>
                           <ColumnDefinition Width="100" />
                           <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                           <RowDefinition />
                           <RowDefinition />
                        </Grid.RowDefinitions>

                        <TextBlock Text="Font Size:" Grid.Column="0" Grid.Row="0" VerticalAlignment="Center" Foreground="{DynamicResource TextForeground}" Margin="0,0,0,5" />
                        <ComboBox Grid.Column="1" Grid.Row="0" ItemsSource="{Binding FontSize}" Margin="0,0,0,5" />

                        <TextBlock Text="Font Family:" Grid.Column="0" Grid.Row="1" VerticalAlignment="Center" Foreground="{DynamicResource TextForeground}" />
                        <ComboBox Grid.Column="1" Grid.Row="1" ItemsSource="{Binding Source={x:Static Member=Fonts.SystemFontFamilies}}" />
                     </Grid>
                  </StackPanel>
               </GroupBox>

               <GroupBox Header="Options" Margin="0,0,0,10">
                  <StackPanel Margin="5">
                     <CheckBox Content="Show Line Numbers" Style="{StaticResource SettingsCheckBox}" Margin="0,0,0,5" />
                     <CheckBox Content="Convert Tabs to Spaces" Style="{StaticResource SettingsCheckBox}" Margin="0,0,0,5" />
                     <CheckBox Content="Enable Email Hyperlinks" Style="{StaticResource SettingsCheckBox}" Margin="0,0,0,5" />
                     <CheckBox Content="Enable Hyperlinks" Style="{StaticResource SettingsCheckBox}" Margin="0,0,0,5" />
                     <CheckBox Content="Enable Code Folding" Style="{StaticResource SettingsCheckBox}" Margin="0,0,0,5" />
                  </StackPanel>
               </GroupBox>

               <GroupBox Header="Indentation" Margin="0,0,0,10">
                  <Grid Margin="5">
                     <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100" />
                        <ColumnDefinition />
                     </Grid.ColumnDefinitions>
                     <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                     </Grid.RowDefinitions>

                     <TextBlock Text="Tab Size:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Foreground="{DynamicResource TextForeground}" />
                     <TextBox Grid.Row="0" Grid.Column="1" Margin="0,0,0,5" MaxLength="2" />

                     <TextBlock Text="Indent Size:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Foreground="{DynamicResource TextForeground}" />
                     <TextBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,5" MaxLength="2" />
                  </Grid>
               </GroupBox>

               <GroupBox Header="Xslt Transformation">
                  <StackPanel Margin="5">
                     <CheckBox Content="Runtime" Style="{StaticResource SettingsCheckBox}" />
                  </StackPanel>
               </GroupBox>
            </StackPanel>
         </ScrollViewer>
      </Grid>
   </Border>
</ct:Window> 