<ct:Window x:Class="XsltEditor.Views.Windows.SettingsView"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"

           xmlns:ct="clr-namespace:XsltEditor.Views.UserControls"

           Height="400"
           Width="350"
           Title="Settings"

           ResizeMode="CanResizeWithGrip"
           WindowStartupLocation="CenterScreen"
           Style="{StaticResource WindowStyle}" AcrylicEffect="False">

   <Border Style="{StaticResource BorderWindow}">
      <Grid>
         <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
         </Grid.RowDefinitions>

         <ct:WindowBar Grid.Row="0" Background="{DynamicResource GeneralPanelBackground}" Title="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}" ShowTitle="True" />

         <ScrollViewer Grid.Row="1" Margin="10" VerticalScrollBarVisibility="Auto">
            <StackPanel>
               <GroupBox Header="Appearance" Margin="0,0,0,10">
                  <Grid Margin="5">
                     <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100" />
                        <ColumnDefinition />
                     </Grid.ColumnDefinitions>

                     <TextBlock Text="Theme:" Grid.Column="0" VerticalAlignment="Center" Foreground="{DynamicResource TextForeground}" />
                     <ComboBox Grid.Column="1" ItemsSource="{Binding Themes}" SelectedItem="{Binding SelectedTheme}" />
                  </Grid>
               </GroupBox>

               <GroupBox Header="Text Editor" Margin="0,0,0,10">
                  <StackPanel>
                     <Grid Margin="5">
                        <Grid.ColumnDefinitions>
                           <ColumnDefinition Width="100" />
                           <ColumnDefinition />
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="Font Family:" Grid.Column="0" VerticalAlignment="Center" Foreground="{DynamicResource TextForeground}" />
                        <ComboBox Grid.Column="1" ItemsSource="{Binding Source={x:Static Member=Fonts.SystemFontFamilies}}" SelectedItem="{Binding FontFamily}" />
                     </Grid>
                  </StackPanel>
               </GroupBox>

               <GroupBox Header="Xslt Transformation">
                  <StackPanel Margin="5">
                     <!--<CheckBox Content="Runtime compile" Margin="0,0,0,5" Style="{StaticResource SettingsCheckBox}" />-->

                     <Grid>
                        <Grid.ColumnDefinitions>
                           <ColumnDefinition Width="100" />
                           <ColumnDefinition />
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="Engine:" Grid.Column="0" VerticalAlignment="Center" Foreground="{DynamicResource TextForeground}" />
                        <ComboBox Grid.Column="1" ItemsSource="{Binding Engines}" SelectedIndex="0" />
                     </Grid>
                  </StackPanel>
               </GroupBox>
            </StackPanel>
         </ScrollViewer>
      </Grid>
   </Border>
</ct:Window> 