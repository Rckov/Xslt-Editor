<ct:Window x:Class="XsltEditor.Views.Windows.MainView"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"

           xmlns:vm="clr-namespace:XsltEditor.ViewModels"
           xmlns:ct="clr-namespace:XsltEditor.Views.UserControls"
           xmlns:ad="https://github.com/Dirkster99/AvalonDock"

           Height="650"
           Width="1200"
           Title="Xslt Editor"

           ResizeMode="CanResizeWithGrip"
           WindowStartupLocation="CenterScreen"
           Style="{StaticResource WindowStyle}" AcrylicEffect="False">

   <ct:Window.InputBindings>
      <KeyBinding Key="G" Modifiers="Control" Command="{Binding OpenGoToLineWindowCommand}" />
   </ct:Window.InputBindings>

   <Border Style="{StaticResource BorderWindow}">
      <Grid>
         <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
         </Grid.RowDefinitions>

         <ct:WindowBar Grid.Row="0" Background="{DynamicResource GeneralPanelBackground}">
            <ct:WindowBar.LeftContent>
               <Menu VerticalAlignment="Center" Background="Transparent">
                  <MenuItem Header="File">
                     <MenuItem Header="Open XSL" Icon="&#xE70B;" />
                     <MenuItem Header="Open XML" Icon="&#xE8C4;" />
                  </MenuItem>

                  <MenuItem Header="Edit">
                     <MenuItem Icon="&#xE7A7;" Command="ApplicationCommands.Undo" Header="Undo" />
                     <MenuItem Icon="&#xE7A6;" Command="ApplicationCommands.Redo" Header="Redo" />
                     <MenuItem Icon="&#xE8C8;" Command="ApplicationCommands.Copy" Header="Copy" />
                     <MenuItem Icon="&#xE8C6;" Command="ApplicationCommands.Cut" Header="Cut" />
                     <MenuItem Icon="&#xE77F;" Command="ApplicationCommands.Paste" Header="Paste" />
                     <MenuItem Icon="&#xE74D;" Command="ApplicationCommands.Delete" Header="Delete" />
                  </MenuItem>

                  <MenuItem Header="Tools">
                     <MenuItem Header="Go To Line" Icon="&#xE933;" Command="{Binding OpenGoToLineWindowCommand}" />
                     <MenuItem Header="Completion" Icon="&#xE75D;" Command="{Binding OpenCompletionWindowCommand}" />
                  </MenuItem>

                  <Separator Style="{StaticResource VerticalSeparator}" Height="20" />

                  <MenuItem Icon="&#xE721;" Command="ApplicationCommands.Find" Header="Find" />
               </Menu>
            </ct:WindowBar.LeftContent>

            <ct:WindowBar.RightContent>
               <Button Content="&#xE713;" FontSize="12" Style="{StaticResource CaptionButton}" ToolTip="Settings" />
            </ct:WindowBar.RightContent>
         </ct:WindowBar>

         <Border Grid.Row="1" Padding="5">
            <ad:DockingManager DocumentsSource="{Binding Documents}" ActiveContent="{Binding ActiveDocument, Mode=TwoWay}" AnchorablesSource="{x:Null}" IsVirtualizingAnchorable="True" IsVirtualizingDocument="True">

               <ad:DockingManager.Resources>
                  <DataTemplate DataType="{x:Type vm:DocumentViewModel}">
                     <ct:TextEditor Text="{Binding Text, Mode=TwoWay}"
                                    IsReadOnly="{Binding IsReadOnly, Mode=TwoWay}"
                                    Line="{Binding Line, Mode=TwoWay}"
                                    Column="{Binding Column, Mode=TwoWay}"
                                    SyntaxHighlighting="{Binding Highlighting, Mode=OneWay}" />
                  </DataTemplate>
               </ad:DockingManager.Resources>

               <ad:LayoutRoot>
                  <ad:LayoutPanel Orientation="Horizontal">
                     <ad:LayoutDocumentPane />

                     <ad:LayoutAnchorablePane DockWidth="300">
                        <ad:LayoutAnchorable Title="Result" CanHide="False" CanAutoHide="False">
                        </ad:LayoutAnchorable>
                     </ad:LayoutAnchorablePane>
                  </ad:LayoutPanel>
               </ad:LayoutRoot>
            </ad:DockingManager>
         </Border>

         <Grid Grid.Row="2" Background="{DynamicResource GeneralPanelBackground}">
            <Grid.ColumnDefinitions>
               <ColumnDefinition />
               <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" Margin="5">
               <TextBlock Text="&#xF745;" Margin="0,0,3,0" FontSize="9" FontFamily="{StaticResource FontIcon}" Foreground="{DynamicResource TextInactiveForeground}" VerticalAlignment="Center" />
               <TextBlock Text="{Binding ActiveDocument.FilePath, TargetNullValue='No file selected'}" Foreground="{DynamicResource TextInactiveForeground}" VerticalAlignment="Center" />
            </StackPanel>

            <Grid Grid.Column="1" HorizontalAlignment="Right">
               <Grid.ColumnDefinitions>
                  <ColumnDefinition />
                  <ColumnDefinition />
                  <ColumnDefinition />
                  <ColumnDefinition />
               </Grid.ColumnDefinitions>

               <TextBlock Text="has changes" VerticalAlignment="Center" Foreground="Gray" Visibility="{Binding ActiveDocument.IsDirty, Converter={StaticResource BooleanToVisibilityConverter}}" />

               <Button Style="{StaticResource StatusBarButton}" Margin="5,0" Grid.Column="1">
                  <TextBlock Foreground="{DynamicResource TextInactiveForeground}">
                     <Run Text="{Binding ActiveDocument.Line, StringFormat='Ln: {0},'}" />
                     <Run Text="{Binding ActiveDocument.Column, StringFormat='Col: {0}'}" />
                  </TextBlock>
               </Button>

               <CheckBox Margin="5,0" ToolTip="Lock/Unlock" IsChecked="{Binding ActiveDocument.IsReadOnly}" Grid.Column="2">
                  <CheckBox.Template>
                     <ControlTemplate TargetType="{x:Type CheckBox}">
                        <Border Name="Bd" Padding="5">
                           <TextBlock Name="IconCheckBox"
                                      FontSize="14"
                                      Foreground="{DynamicResource TextInactiveForeground}"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Center" />
                        </Border>

                        <ControlTemplate.Triggers>
                           <Trigger Property="IsChecked" Value="True">
                              <Setter TargetName="IconCheckBox" Property="TextBlock.Text" Value="Read Only" />
                           </Trigger>
                           <Trigger Property="IsChecked" Value="False">
                              <Setter TargetName="IconCheckBox" Property="TextBlock.Text" Value="Write Mode" />
                           </Trigger>
                           <Trigger Property="IsMouseOver" Value="True">
                              <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ButtonHoverBackground}" />
                           </Trigger>
                        </ControlTemplate.Triggers>
                     </ControlTemplate>
                  </CheckBox.Template>
               </CheckBox>

               <TextBlock Text="{Binding ActiveDocument.Encoding.BodyName, FallbackValue=UTF-8}"
                          Margin="5,0"
                          Grid.Column="3"
                          VerticalAlignment="Center"
                          Foreground="{DynamicResource TextInactiveForeground}" />
            </Grid>
         </Grid>
      </Grid>
   </Border>
</ct:Window>